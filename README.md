# IRG_school21
Платформа «Умный реестр подписок» (Backend-ядро и AI-интеграция) для автоматизации учёта, расчёта дат списаний и контроля статуса подписок пользователя.

```text
subscription-agent/
├── src/
│   ├── __init__.py
│   ├── main.py              # Точка входа, запуск агента с REPL-интерфейсом
│   ├── tools.py             # Набор инструментов (get_obligations, convert_currency)
│   └── obligations.json     # Фикстура базы данных (13 тестовых записей подписок)
├── tests/
│   ├── __init__.py
│   └── test_tools.py        # Набор модульных тестов для валидации логики инструментов
├── Dockerfile               # Инструкции сборки легковесного контейнера
├── docker-compose.yml       # Оркестрация контейнера и окружения
├── .env                     # Конфигурация переменных среды и API-ключей
├── requirements.txt         # Список зависимостей проекта
└── README.md                # Документация проекта
```


# Умный реестр подписок (AI-агент)

Backend-ядро с AI-агентом на базе LangChain, который отвечает на вопросы о подписках:
- подсчёт трат за период в любой валюте,
- определение самой дорогой категории,
- поиск платежей на неделе.

Агент использует ReAct-подход, вызывая инструменты `get_obligations` и `convert_currency`, и логирует весь цикл рассуждений.

## Запуск

1. Клонируйте репозиторий.
2. Скопируйте `.env.example` → `.env` и укажите ваш `OPENAI_API_KEY`.
3. Выполните:
   ```bash
   docker compose up --build

   Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- API-ключ DeepSeek

### Быстрый старт

1. **Клонируйте репозиторий:**

   git clone <repo-url>
   cd subscription-agent-deepseek

2. **Отредактируйте .env, вставьте ваш DEEPSEEK_API_KEY**

3. **Запустите агента**

    docker compose up --build


Модель: deepseek-chat

**Причины выбора:**

1. Отличное качество на русском языке — DeepSeek показывает результаты, сравнимые с GPT-4, но значительно дешевле.
2. Цена — в 10-15 раз дешевле GPT-4o при сопоставимом качестве рассуждений.
3. Function Calling / Tool Use — нативно поддерживает вызов инструментов, что критично для ReAct-агентов.
4. Длинный контекст (128K токенов) — позволяет обрабатывать большие списки обязательств без потери контекста.
5. Открытость — можно использовать как облачный API, так и открытую модель DeepSeek-V3 при необходимости.
6. Стабильность — низкий процент галлюцинаций благодаря строгому следованию инструкциям в промпте.

**Недостатки:**

1. Меньше сообщества и примеров по сравнению с OpenAI (но быстро растёт).
2. Иногда требуется более тщательный промпт-инжиниринг для русскоязычных ответов.

